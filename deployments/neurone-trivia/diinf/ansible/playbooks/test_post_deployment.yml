---
- name: Prueba del rol post-deployment
  hosts: localhost
  connection: local
  gather_facts: no
  vars:
    admin_username: "interaction"
    admin_email: "triviainteraction@gmail.com"
    admin_password: "interaction.2025"
  
  tasks:
    - name: Verificar que docker-compose esté corriendo
      shell: docker-compose ps
      args:
        chdir: /home/thecariah/projects/uni/trivia-test
      register: compose_status
      
    - name: Mostrar estado de contenedores
      debug:
        var: compose_status.stdout_lines
    
    - name: Ejecutar configuración post-deployment
      include_role:
        name: post-deployment